spring:
  application:
    name: jaegokok
  profiles:
    group:
      local: "local"
      dev: "dev"
      prod: "prod"
    include:
      - datasource
      - redis
      - security
      - docs
      - s3
      - mail
      - toss

server:
  port: 8080

qr:
  base-url: ${QR_BASE_URL:http://localhost:3000/scan}
  # font-path: /path/to/NotoSansKR-Regular.ttf  # 한글 폰트 경로 (미설정 시 Latin-1 폴백)

app:
  base-url: ${APP_BASE_URL:http://localhost:3000}

