spring:
  config:
    activate:
      on-profile: "security"

jwt:
  secret-key: ${JWT_SECRET:local-dev-secret-key-not-for-production-must-be-256-bits-long}
  access-token-expiry-ms: ${JWT_ACCESS_EXPIRY_MS:3600000}
  refresh-token-expiry-ms: ${JWT_REFRESH_EXPIRY_MS:604800000}
